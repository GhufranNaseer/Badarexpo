================================================================================
BADAR EXPO SOLUTIONS (BXSS) - REPOSITORY ARCHITECTURE & RULES
================================================================================
Version: 2.0.0
Supersedes: 1.0.0
Objective: Long-term scalability, zero technical debt, and a clean migration
           path from static HTML/CSS/JS -> Custom WordPress Theme.
--------------------------------------------------------------------------------

[0. HOW TO USE THIS FILE]
- This is the CONSTITUTION of the repository. Every AI session and every
  developer session must read this file FIRST, before touching any code.
- Reading order for any new session: rules.txt -> website_structure.txt ->
  data.txt (last 1-2 sessions only) -> the specific file being edited.
- If a rule here conflicts with a request in chat, the AI must flag the
  conflict instead of silently breaking the rule.

--------------------------------------------------------------------------------
[1. REPOSITORY STRUCTURE & FILING]
--------------------------------------------------------------------------------
- ROOT: One HTML file per page. No sub-folders for pages (flat structure is
  intentional -> maps 1:1 to future WordPress page-templates).
- GLOBAL COMPONENTS: Markup/styles/scripts shared by every page (Navbar,
  Topbar, Footer, Mega Menu, Search Overlay) MUST live in dedicated global
  files only:
    * assets/css/navbar.css
    * assets/css/tokens.css      (design tokens - color/radius/shadow/spacing/
                                   motion - added Session 21, loads right
                                   after navbar.css on every page)
    * assets/css/footer.css
    * assets/css/page-hero.css   (shared page-header/breadcrumb banner)
    * assets/js/main.js          (nav, language switcher, search overlay)
- PAGE-SPECIFIC STYLES/SCRIPTS: Anything unique to one page MUST live in a
  file named exactly after that page:
    * index.html                -> assets/css/index.css               assets/js/home.js
    * services.html             -> assets/css/services.css            assets/js/services.js
    * construction-design.html  -> assets/css/construction-design.css assets/js/construction-design.js
    * exhibitionmanagement.html -> assets/css/exhibitionmanagement.css assets/js/exhibitionmanagement.js
    * growthservices.html       -> assets/css/growthservices.css      assets/js/growthservices.js
    * events.html                -> assets/css/events.css              assets/js/events.js
    * contact.html                -> assets/css/contact.css             assets/js/contact.js
    * event-landing.html          -> assets/css/event-landing.css       assets/js/event-landing.js
      (REUSABLE TEMPLATE, added Session 22 - master page for individual event
      landing pages. Duplicate all 3 files per event; only edit copies, never
      the template itself, unless the change should apply to every future event.)
    * events-calendar.html        -> assets/css/events-calendar.css     assets/js/events-calendar.js
      (Event Discovery/Calendar page, added Session 26 - Phase 5. Page-specific
      layout + sample dataset only; the reusable search/filter/calendar logic
      lives in the eds- library files, not here.)
    * industries.html             -> assets/css/industries.css          (no JS - static grid)
      (Industries Hub, added Session 30 - Phase 10. No Discovery Engine -
      finite, slow-growing taxonomy per site-information-architecture.md
      Section 1. No page-specific JS needed; event-components.css/library
      handles all interactivity that exists on this page.)
    * industry-landing.html       -> assets/css/industry-landing.css    (no JS)
      (REUSABLE TEMPLATE, added Session 30 - master page for every future
      industry, same Hub+Template pattern as event-landing.html. Populated
      with real Textile & Apparel content this session as the first
      production page. Duplicate per industry; only edit copies, never this
      template itself, unless the change should apply to every industry.)
    * international-projects.html -> assets/css/international-projects.css (no JS)
      (Projects Hub, added Session 31 - Phase 11. Simple grid at launch, same
      "no Discovery Engine until ~15-20 items" reasoning as industries.html.)
    * project-landing.html        -> assets/css/project-landing.css     (no JS)
      (REUSABLE TEMPLATE, added Session 31 - master page for every future
      international project, same Hub+Template pattern. Populated with real
      China Trade Delegation content as the first production project.)
    * insights.html                -> assets/css/insights.css            (no JS)
      (Insights Hub, added Session 32 - Phase 12. The PARENT knowledge-center
      page - Gallery/Media/Blog are NOT built yet; this page's 3 preview
      sections for them use the Empty State component honestly. Industry
      Insights and Event Insights sections are real, live cross-links into
      the Industries (Phase 10) and Event (Phase 1-6) systems.)
    * gallery.html                 -> assets/css/gallery.css             assets/js/gallery.js
      (Gallery Hub, added Session 33 - Phase 13. Powered by the Phase 5
      EdsDiscovery engine, generalized this session via fieldAccessors/
      cardRenderer options - the first real proof the engine works with a
      non-event dataset. Masonry grid via the new .eds-masonry-grid utility.)
    * gallery-album.html           -> assets/css/gallery-album.css       (no JS)
      (REUSABLE TEMPLATE, added Session 33 - master page for every future
      photo album. Populated with a real China Trade Delegation album,
      cross-linked into the real Project/Industry/Event pages. Lightbox-
      ready markup (data-full-src/data-caption) - no lightbox JS exists yet,
      architecture only per this phase's brief.)
    * media.html                   -> assets/css/media.css               assets/js/media.js
      (Media Center Hub, added Session 34 - Phase 14. Third dataset the
      generalized EdsDiscovery engine has been proven against, via the new
      EdsMediaFields/EdsMediaCard pair.)
    * media-detail.html            -> assets/css/media-detail.css        (no JS)
      (REUSABLE TEMPLATE, added Session 34 - master page for every future
      press release/news item. Populated with the homepage's real existing
      IDEAS 2024 "Newsroom" story content, not invented copy.)
    * blog.html                    -> assets/css/blog.css                assets/js/blog.js
      (Blog Hub, added Session 35 - Phase 15. Fourth dataset the
      generalized EdsDiscovery engine has been proven against, via the new
      EdsBlogFields/EdsBlogCard pair. The LAST content type in Phase 9's
      implementation order - every cross-link here and on blog-post.html
      is a real, live destination.)
    * blog-post.html               -> assets/css/blog-post.css           (no JS)
      (REUSABLE TEMPLATE, added Session 35 - master page for every future
      article. Populated with the homepage's real existing "Blog"-tagged
      story, expanded into real body copy, an author bio reusing
      .eds-speaker-card, and a JSON-LD Article schema block for SEO
      readiness.)
- SHARED "FEATURE" COMPONENTS used on 2+ pages but NOT on every page (e.g.
  CEO Section, Clients Marquee) get their OWN dedicated file, named after the
  component, not the page:
    * assets/css/ceo_section.css
    * assets/css/clients_marquee.css
  Rule of thumb: 1 page only -> page file. 2+ pages -> component file.
- COMPONENT LIBRARIES (broader than a single feature component - a full set
  of reusable UI primitives meant for many future pages/CMS blocks) get
  their own file pair and their own prefix, scoped with a class (not an id,
  since a library component may appear more than once per page):
    * assets/css/event-components.css  (Event Design System, prefix "eds-",
      scope class ".eds-scope" - added Session 24)
    * assets/js/event-components.js    (same library's JS - generic,
      multi-instance, data-attribute-driven modules, NOT id-based)
  See event-design-system.md for the full component catalog.
- ASSETS: assets/images, assets/js, assets/css only. No inline <style> blocks
  in production HTML except where already documented as legacy (to be
  migrated). No images committed outside assets/images.
- NEVER create a new top-level folder without updating website_structure.txt
  in the same session.

--------------------------------------------------------------------------------
[2. FILE NAMING CONVENTIONS]
--------------------------------------------------------------------------------
- HTML/CSS/JS FILES: kebab-case, matches the page/component it represents
  (e.g. construction-design.html, construction-design.css).
- CSS CLASSES: kebab-case, BEM-lite, and PAGE-SCOPED with a short prefix to
  guarantee zero collisions across 100+ future pages:
    * Construction & Design page   -> cd-
    * Services (parent) page       -> svc-
    * Exhibition Management page   -> em-  (adopt going forward)
    * Growth Services page         -> gs-  (adopt going forward)
    * Events page                  -> ev-  (adopt going forward)
    * Contact page                 -> ct-  (adopt going forward)
    * Event Landing Page template  -> elp- (added Session 22 - reusable
      individual-event template, distinct from events.html's own "evtpg-"
      prefix used by the Events portfolio/listing page)
    * Event Design System (EDS) library -> eds- (added Session 24 - the
      standalone component library, distinct from elp- since eds- components
      can appear multiple times per page and are not tied to one page's
      markup at all. Extended Session 26/Phase 5 with search/filter/
      calendar/discovery primitives - same prefix, same scope rules.)
    * Event Calendar / Discovery page -> evc- (added Session 26 - page-layout
      prefix only; the page's actual UI components are all eds-)
    * Industries Hub                  -> ind- (added Session 30 - page-layout
      prefix only, per the same pattern as evc-)
    * Industry Detail master template -> indl- (added Session 30 - distinct
      from ind- since this is a different page/duplication lifecycle)
    * International Projects Hub      -> ipr- (added Session 31 - same
      page-layout-only pattern as ind-)
    * Project Detail master template  -> prjl- (added Session 31 - distinct
      from ipr-, same reasoning as indl- vs. ind-)
    * Insights Hub                    -> insg- (added Session 32 - same
      page-layout-only pattern; this page has no matching "detail template"
      sibling yet since it IS the parent, not a duplicated-per-item page)
    * Gallery Hub                     -> gal- (added Session 33 - same
      page-layout-only pattern as ind-/ipr-)
    * Album Detail master template    -> gala- (added Session 33 - distinct
      from gal-, same reasoning as indl- vs. ind-)
    * Media Center Hub                -> med- (added Session 34 - same
      page-layout-only pattern as gal-)
    * Media Detail master template    -> medd- (added Session 34 - distinct
      from med-, same reasoning as gala- vs. gal-)
    * Blog Hub                        -> blg- (added Session 35 - same
      page-layout-only pattern as med-)
    * Blog Detail master template     -> blgp- (added Session 35 - distinct
      from blg-, same reasoning as medd- vs. med-)
  Global components (navbar, footer, page-hero) stay UNPREFIXED since they
  are singletons that exist on every page (e.g. .navbar, .footer, .page-hero).
- IDs: reserved for JS hooks and in-page anchors only, never for styling.
  ID naming mirrors the class prefix (e.g. #cd-faq-panel-1).
- JS FUNCTIONS/VARIABLES: camelCase. Module-level IIFEs or namespaced objects
  only (see Section 5) - no unprefixed global variables.
- IMAGE FILES: kebab-case, descriptive, no spaces (e.g. stall-design-01.jpg,
  not IMG_2931.jpg). Rename on import if the source file is not compliant.

--------------------------------------------------------------------------------
[3. CSS PHILOSOPHY (BEM-LITE + DESIGN TOKENS)]
--------------------------------------------------------------------------------
- ALL color, spacing, radius, shadow and transition values MUST come from
  CSS custom properties, never hardcoded hex/px in component rules.
- CURRENT GLOBAL TOKENS: as of Session 21, canonical tokens live in
  assets/css/tokens.css (see that file's header comment for full list and
  scope notes). navbar.css :root below is kept for backward compatibility
  and still matches tokens.css exactly - do not let the two drift.
  ORIGINAL NOTE (pre-Session 21, defined in navbar.css :root - the de-facto
  global token file until tokens.css was introduced, see Section 13):
    --primary-red:        #e41f26
    --text-dark:           #1a1a1a
    --text-muted:           #555
    --bg-light:             #f9f9f9
    --white:                #ffffff
    --transition-smooth:    0.35s cubic-bezier(0.4, 0, 0.2, 1)
    --transition-fast:      0.2s cubic-bezier(0.4, 0, 0.2, 1)
    --shadow-soft:          0 15px 40px rgba(0,0,0,0.08)
    --border-light:         rgba(0,0,0,0.05)
  Any NEW page/component that needs a token not listed here must ADD it to
  this root block (not invent a local duplicate) and log it in data.txt.
- Avoid magic numbers for widths/heights where a token or clamp() can do
  the job instead.
- Property order priority inside a rule block: 1. Transform/Opacity (perf)
  2. Flex/Grid layout 3. Box model (padding/margin/border) 4. Visual
  (color/background/shadow) 5. Everything else.
- SPACING SYSTEM: standardize on an 8px base scale going forward
  (8/16/24/32/48/64/96/128px) for any new section. Do not introduce new
  arbitrary spacing values (e.g. 13px, 27px) unless matching an existing
  legacy section pixel-for-pixel.
- TYPOGRAPHY: Primary font is 'Inter' (400/500/600/700/800), loaded via
  Google Fonts with preconnect in <head>. Any section using a secondary
  display font (e.g. Playfair Display, Cormorant Garamond, DM Sans) must
  document WHY in a CSS comment - the long-term goal is to converge on the
  Inter-based red/black/white system for consistency across the site.

--------------------------------------------------------------------------------
[4. RESPONSIVE DESIGN RULES]
--------------------------------------------------------------------------------
- MOBILE-FIRST intent, desktop-first implementation is acceptable for now but
  must converge on a SINGLE shared breakpoint scale going forward:
    * Desktop:        > 1024px
    * Tablet:          768px - 1024px
    * Large Mobile:    480px - 768px
    * Small Mobile:    < 480px
- KNOWN DEBT: existing pages use inconsistent breakpoints (992px, 1200px,
  1300px, 640px, 420px, etc. all appear across the codebase). Do NOT invent
  another one-off breakpoint. New sections must snap to the 4-tier scale
  above; legacy breakpoints are grandfathered until that section is
  refactored.
- Always pair layout breakpoints with a `@media (prefers-reduced-motion:
  reduce)` block for any section using non-trivial animation.
- Always test 320px (smallest common phone width) explicitly.

--------------------------------------------------------------------------------
[5. JAVASCRIPT STANDARDS]
--------------------------------------------------------------------------------
- MODULAR LOGIC: JS must be grouped by functionality (Navigation, Animations,
  Form Handling, Sliders, Filters) using labeled section comments (see
  Section 6).
- Every page-specific JS file must be wrapped in an IIFE `(function(){...})()`
  to avoid leaking variables/functions into the global scope and to avoid
  collisions with main.js or other page scripts if a page is later merged.
- Global/shared behavior (navbar toggle, mega menu, language switcher,
  search overlay) stays in main.js ONLY. Never duplicate this logic inside a
  page-specific file.
- Use `DOMContentLoaded` (or defer + end-of-body placement, already the
  convention) rather than inline onclick="" attributes in HTML.
- Prefer `IntersectionObserver` for scroll-reveal/animate-on-scroll effects
  over scroll event listeners, for performance.
- No inline <script> blocks in production pages except tiny, page-critical
  bootstrapping (e.g. structured data JSON-LD). All behavioral JS belongs in
  assets/js/*.js.

--------------------------------------------------------------------------------
[6. COMMENT CONVENTIONS]
--------------------------------------------------------------------------------
Every major section MUST be demarcated with standardized comment blocks:
  * HTML: <!-- ========================= SECTION NAME ========================= -->
  * CSS:  /* ========================= SECTION NAME ========================= */
  * JS:   // ========================= LOGIC GROUP =========================

WORDPRESS-READY MARKER (already adopted on index.html - extend to every page
going forward): wrap each reusable HTML section with future template-part
markers so the eventual WP conversion is a mechanical extraction, not a
rewrite:
  <!-- ========================= [TEMPLATE PART: section-name.php START] ========================= -->
  ... section markup ...
  <!-- ========================= [TEMPLATE PART: section-name.php END] ========================= -->
Use [TEMPLATE: header.php] / [TEMPLATE: footer.php] for the two global
wrappers, and [TEMPLATE PART: x.php] for every reusable section in between.

--------------------------------------------------------------------------------
[7. ACCESSIBILITY RULES]
--------------------------------------------------------------------------------
- Semantic HTML always: <nav>, <header>, <main>, <section>, <article>,
  <footer>, not generic <div> soup.
- Every <img> needs a descriptive alt attribute (empty alt="" only for
  purely decorative images).
- Interactive elements (menu toggles, accordions, modals, sliders) need
  correct aria-* attributes (aria-expanded, aria-controls, aria-label) and
  must be operable by keyboard (Tab/Enter/Escape), not mouse-only.
- Color contrast: body text on --bg-light / --white backgrounds must meet
  WCAG AA (4.5:1) against --text-dark / --text-muted.
- Respect `prefers-reduced-motion` for all non-essential animation.
- Forms (contact.html) need associated <label> elements, not placeholder-only
  labeling.

--------------------------------------------------------------------------------
[8. PERFORMANCE RULES]
--------------------------------------------------------------------------------
- Use `loading="lazy"` on all below-the-fold images; hero/first-viewport
  images stay eager.
- Use `will-change` sparingly and only on elements with active, complex
  transitions (remove after the interaction, don't leave it on static
  elements).
- Prefer CSS transform/opacity for animation over top/left/width/height.
- Compress and correctly size images before adding to assets/images (see
  Section 10). No unoptimized multi-MB source images in the repo.
- Videos in /Media are reference/marketing assets, not to be embedded
  directly without compression + poster image + lazy-loading strategy.
- Keep CSS/JS files free of dead/unused rules from earlier iterations;
  delete, don't comment-out-and-leave.

--------------------------------------------------------------------------------
[9. SEO RULES]
--------------------------------------------------------------------------------
- Every page needs a unique <title>, meta description, canonical URL, and
  Open Graph / Twitter Card block (already established pattern on index.html
  - replicate on every page).
- One <h1> per page, logical heading hierarchy after that (no skipped
  levels).
- Structured data (JSON-LD: Organization, LocalBusiness, BreadcrumbList,
  Event where relevant) to be added per page as content stabilizes.
- Descriptive, keyword-relevant alt text doubles as an SEO and accessibility
  win - do both with one attribute.

--------------------------------------------------------------------------------
[10. IMAGE, SVG & ICON HANDLING]
--------------------------------------------------------------------------------
- Raster images live under assets/images/<category>/ (logo, portfolio,
  clients-logo, etc.). Keep this folder categorized as it grows - do not
  dump new images loose into assets/images root.
- Prefer .webp for photographic content where feasible; keep .png only where
  transparency is required and .svg is not possible.
- Icons: Font Awesome (already loaded via CDN) is the default icon system.
  Use inline SVG only for custom/brand marks that Font Awesome cannot
  provide - never rasterize a simple icon.
- SVGs used decoratively get `aria-hidden="true"`; SVGs conveying meaning get
  a <title> or aria-label.

--------------------------------------------------------------------------------
[11. COMPONENT PHILOSOPHY & REUSABLE SECTIONS]
--------------------------------------------------------------------------------
- Before building a new section, check if an equivalent pattern already
  exists (card grid, accordion/FAQ, stat strip, testimonial slider, marquee,
  bento layout, before/after compare slider, timeline). Reuse the pattern
  and its CSS conventions rather than inventing a parallel one.
- A section is a good candidate for extraction into a shared component file
  (Section 1) once it is used, or is planned to be used, on 2+ pages.
- Every new component should be built assuming it will become a Gutenberg
  block or ACF Flexible Content layout later (Section 13) - i.e. self-
  contained markup, no dependency on sibling sections' internal state.

--------------------------------------------------------------------------------
[12. CONFLICT PREVENTION & MERGE STRATEGY]
--------------------------------------------------------------------------------
- New classes, IDs, and JS identifiers must never collide with existing ones.
  Use the page-prefix system (Section 2) as the first line of defense.
- Never overwrite, rename, or delete existing classes/functions/sections
  without the user explicitly requesting it in that session.
- When integrating externally-generated code (see
  website_integration-material.txt), always grep the target file for the
  incoming class/ID names first to confirm no collision before pasting.
- Git is already initialized for this repo. Recommended flow going forward:
  one commit per completed section/feature, descriptive commit messages
  (e.g. "feat(construction-design): add before/after compare slider"),
  never commit build artifacts or dependency folders (none currently exist -
  keep it that way while the project stays static).

--------------------------------------------------------------------------------
[13. WORDPRESS PREPARATION PRINCIPLES]
--------------------------------------------------------------------------------
- Do NOT begin WordPress conversion work now. This section exists so every
  decision taken today avoids creating rework later.
- Template-part comment markers (Section 6) must be present on every section
  of every page before conversion begins.
- header.php / footer.php mapping is already implied by the existing
  [TEMPLATE: header.php] / [TEMPLATE: footer.php] markers in index.html -
  replicate the exact same header/footer markup and markers on every other
  page (do not let per-page navbars drift out of sync).
- Global CSS (navbar.css, footer.css, page-hero.css, and a future
  tokens.css) maps to theme-wide enqueued styles. Page CSS (index.css,
  services.css, etc.) maps to conditionally-enqueued template-specific
  styles. Component CSS (ceo_section.css, clients_marquee.css) maps to
  block/template-part specific styles enqueued only where the part is used.
- Repeated content blocks (Industries list, Services mega-menu structure,
  Testimonials, Clients marquee, Stats) are CPT/ACF Repeater candidates -
  flag them, don't hardcode them deeper than necessary.
- Full detail belongs in a dedicated future document (wordpress-roadmap.txt)
  once page content is finalized - this section is the running checklist
  until then.

--------------------------------------------------------------------------------
[14. AI DEVELOPMENT RULEBOOK]
--------------------------------------------------------------------------------
Any AI (this assistant or another) working on this repository must:
  1. Read rules.txt, website_structure.txt, and the last 1-2 data.txt
     sessions before making any change.
  2. NEVER destroy or restructure existing architecture without explicit
     instruction.
  3. NEVER overwrite unrelated code while completing a requested task.
  4. Always work within the naming/prefix/comment conventions of this file,
     even if faster shortcuts are possible.
  5. Always state, at the end of a response, which files were changed and
     what was added/removed in each (see Section 15 checklist).
  6. When integrating externally-supplied code (site-material.txt or pasted
     snippets), follow website_integration-material.txt exactly.
  7. When uncertain which page/section a request applies to, ASK before
     guessing.
  8. Treat this file as append/update-able but never silently contradict it
     - if a new pattern is adopted, this file must be updated in the same
     session (Section 15).

--------------------------------------------------------------------------------
[15. SESSION WORKFLOW & CHECKLISTS]
--------------------------------------------------------------------------------
BEFORE ENDING ANY SESSION:
  1. Verify new code follows section-wise organization and naming/prefix
     rules above.
  2. Clean up ad-hoc/duplicate styles introduced during the session.
  3. Append a new dated entry to data.txt (append-only, never overwrite or
     delete historical logs).
  4. Update rules.txt if a new pattern/token/prefix was adopted this
     session (keep this file the single source of truth).
  5. Update website_structure.txt if any file/folder was added, removed, or
     repurposed.
  6. State clearly in the final response which files are NEW vs MODIFIED
     vs UNCHANGED - never regenerate or resend unchanged files.

CODE REVIEW CHECKLIST (self-check before delivering code):
  [ ] Correct file per Section 1 (global vs page vs component)
  [ ] Naming/prefix rules followed (Section 2)
  [ ] No hardcoded values that should be tokens (Section 3)
  [ ] Responsive across the 4-tier breakpoint scale (Section 4)
  [ ] JS wrapped/scoped, no global leaks (Section 5)
  [ ] Section comment markers + template-part markers present (Section 6)
  [ ] Alt text, aria attributes, keyboard support present (Section 7)
  [ ] Lazy-loading and animation-perf rules followed (Section 8)
  [ ] No collisions with existing classes/IDs/functions (Section 12)

--------------------------------------------------------------------------------
[16. TECHNICAL DEBT & BACKUP STRATEGY]
--------------------------------------------------------------------------------
- Known current debt (tracked, not urgent): inconsistent breakpoint values
  across page CSS files (Section 4); some component prefixes not yet
  adopted on older pages (em-, gs-, ev-, ct- - Section 2); design tokens
  formally defined in navbar.css AND assets/css/tokens.css (Session 21) but
  NOT yet consumed by most legacy page CSS, which still hardcodes hex/px
  values locally.
- SESSION 21 DESIGN SYSTEM AUDIT FINDINGS (full detail in data.txt):
  1. RADIUS SPRAWL: legacy CSS uses ~20 distinct border-radius values (0,
     3, 4, 6, 7, 8, 10, 12, 14, 15, 16, 18, 20, 24, 28, 30, 50, 58, 60,
     100px, plus 50% and 999px for circles/pills). tokens.css now defines a
     6-step scale (xs/sm/md/lg/xl/pill) for new work to snap to. Not
     retrofitted onto existing components in this session (regression risk
     without visual QA per page).
  2. SHADOW SPRAWL: ~45 distinct box-shadow strings in active use, mostly
     unique rgba() combinations rather than a shared scale. tokens.css now
     defines --shadow-xs/sm/soft/md/lg (neutral) and --shadow-red-sm/md/lg
     (brand glow) to consolidate future use.
  3. DUPLICATE COMPONENT CSS: index.css contains a full legacy copy of the
     .ceo-section rules (~450 lines) that predates ceo_section.css being
     extracted into its own shared component file. ceo_section.css loads
     after index.css with equal specificity, so it always wins - the
     index.css copy is dead code today, kept only as an unstyled-content
     fallback for the print/onload CSS-loading trick. Its one color drift
     (--badge-text) was corrected to match the live file this session; the
     block itself was NOT deleted (see inline comment in index.css).
  4. UNDOCUMENTED SECONDARY FONT: 'Playfair Display' (ceo_section.css,
     index.css legacy block) lacked the "WHY" comment required by Section 3.
     Added this session.
  5. TRANSITION DURATION SPREAD: 0.1s-1.5s in use across the codebase; not
     all one-off durations are bugs (some are intentional for
     marquees/reveals), but hover/click micro-interactions specifically use
     0.2s/0.3s/0.35s/0.4s interchangeably for what look like equivalent
     purposes. Logged for a future dedicated interaction-timing pass, not
     touched this session.
  6. BUTTON STYLES: no single shared .btn-primary/.btn-secondary component
     exists; each page defines its own button rules locally. Deferred to a
     dedicated component-extraction task (Section 11) rather than attempted
     blind across 9 pages in one pass.
- Do not silently "fix" legacy debt while doing unrelated work - log it here
  or in data.txt instead, and fix it as its own explicit task.
- Repository is git-tracked; that is the backup strategy for code. Large
  binary assets (Media/*.mp4) should be reviewed periodically for whether
  they belong in git history long-term or should move to external hosting
  once the WordPress phase begins.

--------------------------------------------------------------------------------
ARCHITECT'S NOTE: Build for the 10th page and the 100th page, not just the
1st. Make it readable for the human, not just the machine.
================================================================================
